# Plumaje Mágico — Cloudflare Pages

Build command:
`npm run build`

Output directory:
`dist`

Framework:
`Vite`

No backend or runtime environment variables are required by the current frontend build.
